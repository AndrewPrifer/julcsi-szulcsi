import React from 'react';
import { action } from '@storybook/addon-actions';

import {{name}} from '.';

export default {
  component: {{name}},
  title: '{{name}}',
};

export const normal = () => (
  <{{name}} />
);
